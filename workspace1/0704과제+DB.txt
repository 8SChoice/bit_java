d3.jsp //어렵;;
hi chart
canvas chart


1. 반시계방향으로 그려지는 원 두개 그리기 과제

2. 캔버스

sqlplus / as sysdba : 시스템 권한으로 들어간다.

sys as sysdba

create user javabook idenrified by test00 default tablespace users;
grant resource, connect, create view to javabook;

conn javabook/test00
show user
select * from tab; // 사전테이블. 안만들었지만 기본적으로 제공해줌
